name: geo_asist_front
description: A geo-location based attendance tracking system built with Flutter.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Básico para funcionalidad actual
  # (Agregar más dependencias gradualmente según necesites)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Add your assets here when needed
  # assets:
  #   - assets/images/
  #   - assets/icons/
