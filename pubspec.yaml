name: geo_asist_front
description: A geo-location based attendance tracking system built with Flutter.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ✅ ACTUALIZADO: HTTP requests 
  http: ^1.5.0                    # Era: ^1.4.0

  # Local storage - Mantenido
  shared_preferences: ^2.5.3

  # JSON serialization - Mantenido
  json_annotation: ^4.9.0

  # State management - Mantenido
  provider: ^6.1.5

  # Background processing - CRÍTICO para tracking continuo
  workmanager:
    git:
      url: https://github.com/fluttercommunity/flutter_workmanager.git
      path: workmanager
      ref: main

  # ✅ ACTUALIZADO: Location services - ACTUALIZACIONES CRÍTICAS
  geolocator: ^14.0.2             # Era: ^14.0.1 - Mejor precisión GPS
  permission_handler: ^12.0.1     # Era: ^12.0.0+1 - Permisos mejorados
  web_socket_channel: ^2.4.0


  # Maps (for future integration) - Mantenido
  google_maps_flutter: ^2.12.3

  # Utils - Compatible with local_auth
  intl: ^0.18.1
  
  # 📋 NUEVO: URL launcher para justificaciones
  url_launcher: ^6.3.1

  # ✅ ACTUALIZADO: Notificaciones push reales - CRÍTICO
  flutter_local_notifications: ^19.4.0  # Era: ^17.2.3 - 2 versiones más nueva
  
  # Manejo de zonas horarias para notificaciones - Mantenido
  timezone: ^0.10.1
  
  # ✅ ACTUALIZADO: Optimización de performance - VERSIONES ESTABLES
  battery_plus: ^6.2.2           # MANTENER - Funciona correctamente
  connectivity_plus: ^6.1.5      # Era: ^6.0.5 - Actualizados bugs de conectividad
  device_info_plus: ^10.1.2      # MANTENER - NO actualizar (breaking changes en 11.x)
  package_info_plus: ^8.0.2      # Mantenido
  
  # Logging avanzado - Mantenido
  logger: ^2.4.0
  
  # Acceso a directorios del sistema - Mantenido
  path_provider: ^2.1.4
  
  # 🔥 FIREBASE DEPENDENCIES - NEW
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.3
  firebase_auth: ^5.3.1
  firebase_messaging: ^15.1.3
  firebase_analytics: ^11.3.3

  # 🔐 SECURITY DEPENDENCIES - Phase 5 Security Services
  crypto: ^3.0.3                      # For request signing and hashing
  encrypt: ^5.0.1                     # For data encryption
  flutter_secure_storage: ^9.0.0     # Secure token storage
  local_auth: ^2.1.6                  # Biometric authentication
  local_auth_android: ^1.0.38        # Android biometric implementation
  local_auth_ios: ^1.1.6             # iOS biometric implementation
  
  # 🌐 NETWORK DEPENDENCIES - Phase 5 Secure API Client
  dio: ^5.3.2                         # Advanced HTTP client
  # Certificate pinning implemented manually in SecureApiClient
  
  # 📊 ANALYTICS & PERFORMANCE - Phase 5 Monitoring
  # device_info_plus: ^10.1.2        # Already included above
  # package_info_plus: ^8.0.2        # Already included above
  
  # 🎨 ANIMATION & UX - Phase 5 Enhanced UX
  # No additional dependencies needed - using built-in Flutter animations

  get_it: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # ✅ ACTUALIZADO: Lints - Reglas modernas 
  flutter_lints: ^6.0.0          # Mantenido - Ya actualizado

  # JSON code generation - Mantenido
  build_runner: ^2.5.4
  json_serializable: ^6.9.5
  
  # 🧪 TESTING DEPENDENCIES - Para Fase B.4
  mockito: ^5.4.4                 # MANTENER - Para unit tests con mocks
  
  # Enhanced testing dependencies for comprehensive coverage
  mocktail: ^1.0.4                # Alternative to mockito without code generation
  fake_async: ^1.3.1              # For testing Timer and Stream operations
  test: ^1.25.8                   # Enhanced testing capabilities
  
  # Widget and Golden testing
  golden_toolkit: ^0.15.0         # For golden file testing
  patrol: ^3.10.0                 # For advanced integration testing
  
  # Network and HTTP mocking
  http_mock_adapter: ^0.6.1       # For mocking HTTP requests
  
  # Additional testing utilities
  leak_tracker_flutter_testing: ^3.0.8  # Memory leak detection

flutter:
  uses-material-design: true

  # ✅ ASSETS: Preparado para futuro uso
  # assets:
  #   - assets/images/
  #   - assets/icons/
  # Para habilitar assets:
  # 1. Crear carpetas assets/images/ y assets/icons/
  # 2. Descomentar líneas arriba  
  # 3. Ejecutar flutter pub get

  # ✅ FONTS: Preparado para tipografía personalizada
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #       - asset: fonts/Roboto-Bold.ttf
  #         weight: 700

# ✅ CONFIGURACIÓN ADICIONAL PARA ANDROID
# flutter:
#   android:
#     min_sdk_version: 21    # Android 5.0+ requerido para geolocalización
#     compile_sdk_version: 34
#     target_sdk_version: 34